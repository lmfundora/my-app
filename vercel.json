{
 "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
 ],
 "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
 ],
 "env": {
    "DATABASE_URL": "@prisma/db"
 },
 "scripts": {
    "build": "npx prisma migrate deploy && next build"
 }
}